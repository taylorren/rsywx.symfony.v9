<!-- Statistics Cards -->
{% if stats %}
<div class="col-md-3 mb-3">
    <div class="card shadow-sm h-100">
        <img src="/images/bookshelf.webp" class="card-img-top" alt="书架" style="height: 200px; object-fit: cover;">
        <div class="card-body d-flex flex-column">
            <h6 class="card-title text-muted mb-3">藏书统计</h6>
            <p class="card-text flex-grow-1">
                截止今日（{{ "now"|date("Y年m月d日") }}），任氏有无轩已经藏书<strong>{{ stats.totalBooks }}</strong>册，总页数<strong>{{ stats.totalPages|number_format }}</strong>页，约<strong>{{ stats.totalKwords|number_format }}</strong>千字。总访问量<strong>{{ stats.totalVisits|number_format }}</strong>次。
            </p>
            <a href="{{ path('books_list') }}" class="btn btn-primary btn-sm mt-auto">
                <i class="bi bi-book"></i> 浏览藏书
            </a>
        </div>
    </div>
</div>
{% endif %}