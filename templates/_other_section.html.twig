{# Other Section - Contains Word of the Day and Quote of the Day #}
<div class="row mb-4">
    <div class="col-12">
        <h4 class="mb-3">
            <i class="bi bi-lightbulb-fill text-primary me-2"></i>每日精选
        </h4>
    </div>
</div>

<div class="row g-4 mb-4">
    {# Word of the Day #}
    <div class="col-lg-6">
        {{ include('_word_of_day.html.twig') }}
    </div>
    
    {# Quote of the Day #}
    <div class="col-lg-6">
        {{ include('_quote_of_day.html.twig') }}
    </div>
</div>