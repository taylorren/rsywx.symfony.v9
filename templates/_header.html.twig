<!-- Navigation -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
    <div class="container">
        <a class="navbar-brand" href="{{ path('app_home') }}">
            <img src="{{ asset('images/logo.svg') }}" alt="RSYWX Logo" style="height: 40px; width: auto; margin-right: 8px;"> 任氏有无轩       </a>
        
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_home') }}">首页</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('books_dashboard') }}">图书</a>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                        浏览
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{ path('books_latest') }}">最新图书</a></li>
                        <li><a class="dropdown-item" href="{{ path('books_random') }}">随机图书</a></li>
                        <li><a class="dropdown-item" href="{{ path('books_recent') }}">最近访问</a></li>
                        <li><a class="dropdown-item" href="{{ path('books_forgotten') }}">遗忘图书</a></li>
                    </ul>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('stats') }}">统计</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('about') }}">关于</a>
                </li>
            </ul>
            
            <!-- Search Form -->
            <form class="d-flex search-box" method="GET" action="{{ path('books_search') }}">
                <input class="form-control me-2" type="search" name="q" placeholder="搜索图书..." aria-label="搜索">
                <button class="btn btn-outline-light" type="submit">
                    <i class="bi bi-search"></i>
                </button>
            </form>
        </div>
    </div>
</nav>